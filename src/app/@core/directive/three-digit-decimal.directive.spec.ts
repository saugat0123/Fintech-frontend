import { ThreeDigitDecimalDirective } from './three-digit-decimal.directive';

describe('ThreeDigitDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeDigitDecimalDirective();
    expect(directive).toBeTruthy();
  });
});

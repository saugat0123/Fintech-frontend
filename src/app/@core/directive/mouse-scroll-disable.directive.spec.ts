import { MouseScrollDisableDirective } from './mouse-scroll-disable.directive';

describe('MouseScrollDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseScrollDisableDirective();
    expect(directive).toBeTruthy();
  });
});

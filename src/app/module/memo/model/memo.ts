import {MemoType} from "./memoType";

export class Memo {
    id: number;
    subject: string;
    // sentBy: User;
    // sentTo: User;
    // cc: Set<User>;
    // bcc: Set<User>;
    content: string;
    status: any;
    // stage: Stage
    // type: MemoType
    // stages: Set<MemoStage>;
}